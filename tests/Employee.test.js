const Employee = require("../lib/Employee")

describe("Employee Name", () => {
    it("should do something", () => {
        expect(new Employee("Kevin")).toBe(String); // TODO: replace placeholder method thing
    });
});